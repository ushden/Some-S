enum RequestQuerySource {
  Body = 'body',
  Query = 'query',
  Plain = 'plain',
}

enum RequestQuerySubSource {
  Where = 'where',
  Filter = 'filter',
}

export {
  RequestQuerySource,
  RequestQuerySubSource,
};
